@model WebApplication2.Models.TestTabViewModel

<form class="my_form" novalidate="novalidate">
    @if (Model.IsEditable)
    {
        @Html.TextBoxFor(o => o.Username)
    }
    else
    {
        <span>@Model.Username</span>
    }
</form>
<script type="text/javascript">
    function initSaveAjaxParams(cnt) {
        return {
            type: "Post",
            dataType: "html",
            // contentType: 'application/json',
            data: cnt.find('.my_form').serialize()
        };
    }
    function initCreateAjaxParams(cnt) {
        return {};
    }
    function afterRefresh(cnt, isEditable) {
        
    }
</script>
